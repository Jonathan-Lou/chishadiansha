<!--index.wxml-->
<view class="container">
  <dish-component 
    visible="{{showEditDish}}" 
    pageType="{{modalType}}"
    menuId="{{menuId}}"
    bind:close="onEditDishClose"
    bind:success="onEditDishSuccess"
  />
</view>